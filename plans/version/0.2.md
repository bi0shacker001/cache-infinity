# CacheInfinity v0.2 Implementation Plan

## Overview

This document defines the intended initialization flow and service boundaries for CacheInfinity v0.2. It reflects the desired runtime sequence and service interface standards, including the integrated database structure reorganization.

## Target Initialization Flow

1. `app/cacheinfinity.py` launches the main server loop in `core.server`
2. `core.server` delegates initialization to `core.services` (and only for this purpose)
3. `core.services` initializes services in the required order:
   - **DatabaseManager** (`db.dbmanage`) first (foundational dependency)
   - **ConfigManager** (`core.config`) second (non-database configuration)
   - **All other services** afterward (order respects dependencies)
4. The error handler in `core.errors` controls error handling and propagation
5. Each service uses a standard interface (`initialize()`, `start()`, `stop()`)

## Core Rules

- `core.server` owns the main loop; it does not construct services directly
- `core.services` only initializes services and coordinates their lifecycle
- `DatabaseManager` must not rely on `core.config` for database settings
- Database settings are supplied at startup; `db.dbmanage` may load `database.yml` itself
- `core.config` handles configuration stored in the database only
- The runtime directory must not live under the config directory (use `/run` or `/var/run` with temp fallback)
- At runtime, config dir usage is limited to logging, TLS cert files, and user-initiated bootstrap import/export
- All config-dir filesystem access must go through `storage.configuration`
- `core.config` must not perform filesystem IO; `db.dbmanage` owns reading `database.yml` and bootstrap content via `storage.configuration`
- Credentials are stored hashed and salted; session authentication uses tokenized sessions (CLI is exempt)
- Cachelinks may specify a `url_handler` (auto/http/ftp/rclone); indexer/fetcher select handlers without mutating URLs
- Rclone settings are database-backed and exposed via admin interfaces (UI/CLI) through `ui.backend`

## Database Structure Reorganization

### Current Problems
1. **Mixed Responsibilities**: `adapter.py` currently handles both routing logic AND database operations
2. **Inconsistent Backend Interfaces**: Each backend has different method signatures and capabilities
3. **Tight Coupling**: Schema operations are tightly coupled to specific database implementations
4. **Redis Integration**: Redis operations are mixed with SQL operations in the adapter

### New Architecture

#### Unified Backend Interface
All backends will implement a common interface with standardized methods:

```python
class DatabaseBackend(Protocol):
    """Unified interface for all database backends."""
    
    def connect(self) -> None: ...
    def close(self) -> None: ...
    def execute(self, sql: str, params: Sequence[Any] = ()) -> Any: ...
    def executemany(self, sql: str, params_list: list[tuple]) -> None: ...
    def fetchone(self, sql: str, params: Sequence[Any] = ()) -> dict | None: ...
    def fetchall(self, sql: str, params: Sequence[Any] = ()) -> list[dict]: ...
    def commit(self) -> None: ...
    def rollback(self) -> None: ...
    def health_check(self) -> bool: ...
    def get_pool_stats(self) -> dict: ...
```

#### Backend Specialization
Each backend will focus on its specific database type:
- **`db.backends.sqlite`**: SQLite-specific operations with file-based storage
- **`db.backends.postgresql`**: PostgreSQL-specific operations with connection pooling
- **`db.backends.redis`**: Redis-specific caching operations only

#### Adapter Responsibilities
The adapter will handle:
- **Routing Logic**: Decide which backend to use based on operation type
- **Caching Strategy**: Determine when to use Redis vs primary database
- **Transaction Management**: Coordinate transactions across backends
- **Error Handling**: Provide unified error handling across backends

### Implementation Phases

#### Phase 1: Backend Interface Unification
1. Create unified backend interface (`db/backends/base.py`)
2. Refactor SQLite backend to follow interface (build on existing work)
3. Refactor PostgreSQL backend to follow interface
4. Refactor Redis backend for caching-only operations

#### Phase 2: Enhanced Adapter and Schema Updates
5. Enhance adapter with improved routing logic (build on existing work)
6. Update schema.py to use new backend interfaces
7. Validate integration with initialization flow changes

#### Phase 3: Testing and Documentation
8. Create comprehensive test suite
9. Update documentation and create migration guide

## Service Order and Responsibilities

### 1. DatabaseManager (`db.dbmanage`)
**Purpose**: Initialize the database layer independently of the configuration manager  
**Dependencies**: None  
**Responsibilities**:
- Initialize database adapter using startup-provided database settings
- Optionally load `database.yml` for database configuration only
- Create schema, run migrations, and open connections
- Provide database interfaces to dependent services
- **NEW**: Use unified backend interfaces for all database operations

### 2. ConfigManager (`core.config`)
**Purpose**: Manage non-database configuration  
**Dependencies**: DatabaseManager  
**Responsibilities**:
- Load application configuration (excluding database settings)
- Validate and expose configuration to other services
- Handle configuration defaults and overrides

### 3. Logging Service (`core.logging`)
**Dependencies**: ConfigManager  
**Responsibilities**:
- Configure logging based on non-database configuration
- Provide logger instances to dependent services

### 4. Credential Manager (`auth.credentials`)
**Dependencies**: ConfigManager, DatabaseManager  
**Responsibilities**:
- Load credential configuration and secrets
- Initialize credential storage and verification
- Store only salted, hashed credentials
- Issue and validate tokenized sessions for authenticated requests (CLI uses API key auth)

### 5. TLS Service (`auth.tls`)
**Dependencies**: ConfigManager  
**Responsibilities**:
- Load TLS certificates and settings
- Initialize TLS handling for network services

### 6. Storage (`storage.datadir`, `storage.staging`)
**Dependencies**: ConfigManager  
**Responsibilities**:
- Initialize datadir registry from database-backed settings
- Initialize staging area and enforce staging-first rules

### 7. Cachelinks (`cache.cachelinks`)
**Dependencies**: ConfigManager  
**Responsibilities**:
- Load cachelinks from configured sources
- Provide cachelink index to indexing and WebDAV layers

### 8. Fetcher (`net.fetcher`)
**Dependencies**: ConfigManager  
**Responsibilities**:
- Initialize the download pipeline and cookie handling
- Provide fetcher instance to indexing and WebDAV layers

### 9. Indexer (`net.indexer`)
**Dependencies**: ConfigManager, DatabaseManager, Logging  
**Responsibilities**:
- Initialize remote indexing logic
- Start indexing workers and schedules

### 10. Checksums (`cache.checksum`)
**Dependencies**: ConfigManager, DatabaseManager  
**Responsibilities**:
- Initialize checksum catalog for cache validation

### 11. Application Service (`core.services`)
**Dependencies**: DatabaseManager, ConfigManager, Logging, Credential Manager, TLS, Storage, Cachelinks, Fetcher, Indexer, Checksums  
**Responsibilities**:
- Assemble the running `CacheInfinityService`
- Start background tasks after initialization

### 12. WebDAV (`hosting.webdav`)
**Dependencies**: Application Service  
**Responsibilities**:
- Build the WsgiDAV application with share mappings and auth

### 13. WebUI (`ui.web.webcore`)
**Dependencies**: Application Service  
**Responsibilities**:
- Build the WebUI application and routing

## Service Interface

```python
from abc import ABC, abstractmethod
from typing import Dict, Any

class BaseService(ABC):
    """Standard interface for all CacheInfinity services."""

    @abstractmethod
    def initialize(self, context: Dict[str, Any]) -> None:
        """Initialize the service with dependencies from prior services."""
        pass

    @abstractmethod
    def start(self) -> None:
        """Start the service after all dependencies are initialized."""
        pass

    @abstractmethod
    def stop(self) -> None:
        """Stop the service and clean up resources."""
        pass
```

## Error Handling

- Errors are managed and surfaced by `core.errors`
- Service initialization failures must propagate to the error handler
- Non-critical startup failures should be logged and handled per service policy

## Implementation Order

This order integrates the initialization refactor with the remaining SPEC-defined work so each component is completed in dependency order.

### Phase 0: Layout and Config-Directory Compliance
1. Move service wiring out of `core.server` so it no longer imports `ui.web.webcore` or `hosting.webdav` (shift that to `core.services`).
2. Remove the `sqlite3` import from `db/schema.py` (SQLite access must stay in `db/backends/sqlite.py`).
3. Route all config-dir filesystem access through `storage.configuration`.
4. Enforce runtime-dir placement under `/run` or `/var/run` (with temp fallback), never under the config directory.
5. Remove filesystem access from `core.config` entirely.
6. Have `db.dbmanage` read/write `database.yml` and bootstrap content via `storage.configuration`, and pass bootstrap data to `db.backupmgmt`.

### Phase 1: Foundation and Service Orchestration
7. Define the `BaseService` interface and standard lifecycle (`initialize`, `start`, `stop`) in `core.services`.
8. Build a `ServiceManager` in `core.services` for dependency ordering, context passing, and lifecycle coordination.
9. Standardize error flow through `core.errors` so service failures propagate consistently.

### Phase 2: Startup Inputs and Config Directory Contract
10. Implement CLI and environment parsing in `core.server` to gather startup inputs, including `--config-dir`, database settings, and bootstrap flags.
11. Apply database connectivity precedence: CLI flags → environment variables → `database.yml` (last resort).
12. Establish the fixed log directory `<config-dir>/logs/` and log level precedence (CLI → env → default `INFO`).

### Phase 3: Database Layer (Foundational Service) - WITH NEW ARCHITECTURE
13. Implement `db.dbmanage` initialization using startup-provided database settings, with `database.yml` only for DB access.
14. Enforce SQLite fixed path `<config-dir>/cacheinfinity.db` and PostgreSQL support via `CACHEINFINITY_DATABASE_URL`.
15. Auto-create/upgrade database tables (targets, files, events, access logs) and wire optional Redis metadata cache.
16. **NEW**: Create unified backend interface (`db/backends/base.py`)
17. **NEW**: Refactor SQLite backend to follow unified interface
18. **NEW**: Refactor PostgreSQL backend to follow unified interface
19. **NEW**: Refactor Redis backend for caching-only operations
20. **NEW**: Enhance adapter with improved routing logic

### Phase 4: Configuration and Bootstrap
21. Implement `core.config` to load non-database configuration from the database only.
22. Add bootstrap YAML import (`--bootstrap`) with validation, best-effort merge, and unknown-key logging.
23. Implement backup/export to durable YAML using the same schema as bootstrap (reverse flow).
24. Persist rclone settings in the database and include them in bootstrap import/export.
   - Include rclone rc endpoint and credentials for `ui.backend` control.

### Phase 5: Logging, Auth, and TLS
25. Configure `core.logging` from non-database config and provide logger instances to services.
26. Initialize `auth.credentials` for users, credentials, and authorization policies stored in the database.
27. Enforce salted+hashed credentials at rest and tokenized session auth for UI/API (CLI continues to use API key auth).
28. Initialize `auth.tls` with TLS modes: manual, http-01, dns-01, external; support reverse-proxy deployments.

### Phase 6: Storage and Datadir Contracts
29. Initialize storage managers in `storage.configuration`, `storage.datadir`, and `storage.staging` from database-backed settings.
30. Enforce datadir precedence rules and staging-first download requirements at the storage boundary.

### Phase 7: Fetcher and Cookie Handling
31. Implement `net.fetcher` as a unified PycURL pipeline with resume, retries, timeouts, and minimum speed.
32. Add handler selection (`url_handler`) to route downloads to HTTP/FTP/rclone without URL rewriting.
33. Wire rclone settings (`enabled`, `config_path`) into fetcher runtime, using `RCLONE_CONFIG` when enabled.
34. Store per-domain cookies in the database as Base64 Netscape payloads; supply cookies to downloads without on-disk jars.
35. Add admin actions (WebUI/CLI) to set/list/delete cookies via inline content and bootstrap import/export (no credential-based refresh).

### Phase 8: Indexing and Availability
36. Implement `net.indexer` scheduler constraints, budgets, and hotness tracking with decay.
37. Persist listing metadata (path, URL, size, mtime, protocol, checksum when available) and support conditional checks.
38. Add handler selection (`url_handler`) for listings and wire rclone config into listing fetches.
39. Mark targets `needs_full_reindex` on 404/5xx during live GET and add availability probing.

### Phase 9: Cachelinks, WebDAV, and Read-Through Caching
40. Implement cachelink parsing and deterministic IDs in `cache.cachelinks` with database-backed persistence.
41. Extend cachelinks with `url_handler` and persist it through bootstrap/import/export.
42. Build WebDAV overlays in `hosting.webdav` with share schema enforcement and per-user flags.
43. Implement staging-first read-through caching, avoid-download rule, and datadir override precedence.
44. Add ZIP caching policy (`max_zip_total_gb`, `one_zip_cache_at_a_time`) with whole-zip and per-file flows.
45. Expose cache state and size-on-disk via DAV live properties (`{urn:cacheinfinity}cache-state`, `{urn:cacheinfinity}size-on-disk`).

### Phase 10: Admin Interfaces and Operations
46. Implement admin WebUI in `ui.web.webcore` and management layer in `ui.backend`.
47. Add rclone settings to the Settings UI and cachelink `url_handler` controls to the Cachelinks UI.
48. Route rclone control through `ui.backend` using rclone's API (rc); do not expose rclone operations via `ui.api`.
49. Implement read-only admin API in `ui.api` authenticated via the admin user model.
50. Implement admin CLI in `ui.cli` (users, cachelinks, bootstrap import, backups, cookies, rclone settings).

### Phase 11: Integration and Deployment Hardening
51. Refactor `core.server` to use `core.services` for initialization and lifecycle control.
52. Validate Docker and systemd layouts: `/config`, `/datadir`, `/staging`, TLS files, and logging.
53. Verify initialization order, error handling propagation, and startup performance targets.
54. **NEW**: Update schema.py to use new backend interfaces
55. **NEW**: Create comprehensive test suite for new database architecture
56. **NEW**: Update documentation and create migration guide

## Progress Log

### Database Structure Reorganization Progress
- **Phase 1.1**: Unified backend interface specification created
- **Phase 1.2**: SQLite backend partially refactored (uses db_path parameter)
- **Phase 1.3**: PostgreSQL backend needs interface implementation
- **Phase 1.4**: Redis backend needs caching-only focus
- **Phase 2.1**: Adapter enhanced with Redis support and operation routing
- **Phase 2.2**: Schema.py needs updates to use new backend interfaces

### Initialization Flow Progress (from v0.2_crunch.md)
- Phase 0: service registration moved into `core.services` via `create_service_manager`; `core.server` now calls it.
- Phase 0: removed `sqlite3` import from `db/schema.py`.
- Phase 0: `core.config` no longer performs filesystem IO; bootstrap migration moved to `BackupService`.
- Phase 0: `db.dbmanage` reads `database.yml` via `storage.configuration`; bootstrap reads route through `dbmanage.load_bootstrap_data`.
- Phase 0: bootstrap import/export now supports text/data flows in `db.backupmgmt` and UI config import uses those paths.
- Phase 7/Phase 0: cookies and credentials no longer use disk storage; UI/CLI cookie credfile/refresh flows removed; fetcher only uses DB cookie content.
- Phase 7: WebUI cookie routes and settings UI now avoid cookie path/credential inputs; bootstrap remains the import/export channel.
- Phase 0: checksum catalog no longer stores or scans catalog files in the config directory; catalog scanning is optional/off by default.
- Phase 0: SQLite backend now uses explicit sqlite path and no longer writes/creates directories outside configuration manager.
- Phase 7: CLI cookie domain add now documents inline cookie content (no on-disk cookie jars).
- Phase 0: TLS directory creation is routed through `storage.configuration`.
- Phase 0: logging directory creation now goes through `storage.configuration`.
- Phase 7: cookie definitions now only store inline content in memory/DB; cookie file/credfile fields removed from config models.
- Phase 7: cookie API payloads no longer include credfile metadata.
- Phase 0: cachelink file loading is disabled; cachelinks are sourced from the database/bootstrap only.
- Phase 0: configuration manager no longer exposes cookie/credentials disk paths.
- Phase 0: backup import/export file IO routed through `storage.configuration`.
- Phase 0: bootstrap path resolution in `core.server` now goes through `storage.configuration`.
- Phase 0: TLS certificate checks/cleanup and database.yml validation now use `storage.configuration` for filesystem access; `core.server` TLS manual checks route through it.

## Benefits of Integration

### 1. **Consistent Architecture**
- All backends follow the same interface
- Clear separation between SQL operations and caching
- Consistent error handling across backends

### 2. **Improved Maintainability**
- Easier to add new database backends
- Cleaner code organization
- Better testability of individual components

### 3. **Enhanced Performance**
- Proper Redis caching strategy
- Optimized operation routing
- Reduced code duplication

### 4. **Better Integration**
- Aligns with service orchestration changes
- Maintains backward compatibility
- Supports both initialization approaches

## Risk Mitigation

### 1. **Backward Compatibility**
- Maintain existing API where possible
- Provide migration paths for existing code
- Ensure database schema compatibility

### 2. **Testing Coverage**
- Comprehensive unit tests for each backend
- Integration tests for adapter routing
- End-to-end tests with real deployments

### 3. **Gradual Rollout**
- Implement changes incrementally
- Test with development environment first
- Clear rollback procedures

## Success Criteria

- [ ] All backends implement unified interface
- [ ] Adapter properly routes operations and caching
- [ ] Schema uses new backend interfaces
- [ ] Database manager works with new architecture
- [ ] Integration with initialization flow complete
- [ ] All existing functionality preserved
- [ ] Performance maintained or improved
- [ ] Comprehensive test coverage
- [ ] Migration guide complete

## Dependencies and Coordination

### Dependencies on v0.2_crunch.md
- Database settings precedence (already implemented)
- Service orchestration order (already defined)
- Filesystem access routing (already implemented)

### Coordination Points
- Ensure database initialization aligns with service startup
- Coordinate testing with initialization flow validation
- Align documentation updates with overall project docs

This integrated plan builds on the excellent work already done while ensuring the database structure reorganization complements the initialization flow improvements.